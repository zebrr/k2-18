# K2-18: Архитектура проекта

Название проекта `K2-18` выбрано в честь звездной системы, в атмосфере экзопланеты которой нашли (предположительно) водяной пар.

## ВВЕДЕНИЕ

### Что делает K2-18

`K2-18` трансформирует неструктурированный образовательный контент (учебники, курсы, документацию) в структурированные семантические графы знаний. Система извлекает концепты с определениями, строит учебные фрагменты, выявляет семантические связи и вычисляет образовательные метрики - создавая навигируемую структуру знаний для адаптивных систем обучения.

### Почему семантические графы

**Классические подходы (с 1960-х)**
- **Ручные онтологии**: эксперты годами строят WordNet, Cyc - дорого и медленно
- **NLP-экстракция**: выделение именованных сущностей (NER), связей - но без понимания образовательных зависимостей
- **Статистические методы**: TF-IDF, LSA, topic modeling - находят темы, но не структуру обучения
- **Универсальные графы знаний** (Google KG, Wikidata): факты и отношения, но не учебные траектории

**Проблемы для образования**
- Не понимают prerequisite chains (что нужно знать ДО изучения темы)
- Не различают типы образовательных связей (пример vs детализация vs проверка знаний)
- Не вычисляют learning effort и оптимальные пути обучения
- Ручное построение требует месяцев работы методистов

**Наш подход на основе современных исследований**
Проект комбинирует достижения в области:
- **iText2KG (Incremental Text to Knowledge Graph)** - современный подход к инкрементальному построению графов знаний из больших текстов, позволяющий обрабатывать документы по частям с сохранением семантической целостности
- **Образовательного data mining** - исследования MOOCCube и KnowEdu по извлечению структуры знаний из учебных материалов
- **Современных LLM** - использование больших языковых моделей для понимания образовательной семантики и выявления неявных зависимостей

## Ключевые инновации `K2-18`

1. **Образовательная семантика**: Не просто "связано с", а 9 специфичных типов связей (PREREQUISITE, ELABORATES, TESTS и др.) - именно то, что нужно методисту для построения учебных траекторий
2. **Инкрементальность с памятью**: Обработка книги в 1000+ страниц через контекстные цепочки LLM с сохранением дальних зависимостей между главами
3. **Двухфазное извлечение**: Раздельное создание словаря концептов и построение графа (в три прохода) - радикально повышает качество и снижает потери контента
4. **Гибридный интеллект**: LLM для понимания смысла + embeddings для семантического сходства + графовые алгоритмы для образовательных метрик
5. **Быстрый результат**: Вместо (месяцев) ручной работы методистов - автоматическое построение за несколько часов с сохранением качества

## ЧАСТЬ 1. ПОСТРОЕНИЕ ГРАФА ЗНАНИЙ

### 1.1. Pipeline: от текста до графа

```
📚 Raw Content (.md, .txt, .html)
    ↓
1. Slicer             → Semantic Chunks
    ↓
2. iText2KG Concepts  → Concept Dictionary
    ↓
3. iText2KG Graph     → Knowledge Graph (raw)
    ↓
4. Dedup              → Knowledge Graph (deduplicated)
    ↓
5. Refiner Longrange  → Knowledge Graph (with long-range connections)
    ↓
🕸️ Knowledge Graph (with Concept Dictionary)
```

#### Slicer

**Что делает**: Разделяет текст на семантически связные фрагменты (5-15К токенов) с учётом границ абзацев, заголовков, предложений.

**Почему важен**: Решает проблему контекстных ограничений LLM (до 128K токенов), сохраняя смысловую целостность. Детерминированная генерация ID обеспечивает повторяемость результатов.

**Вход/выход**: 
- Вход: `/data/raw/*` (.txt, .md, .html, .json)
- Выход: `/data/staging/*.slice.json`

**Детали**: `/docs/specs/cli_slicer.md`

#### iText2KG Concepts

**Что делает**: Последовательно обрабатывает слайсы, извлекая и канонизируя концепты. Использует previous_response_id для сохранения контекста между слайсами.

**Почему важен**: Раздельное извлечение концептов от построения графа снижает когнитивную нагрузку на LLM. Монолитный подход терял до 2/3 контента.

**Вход/выход**:
- Вход: `/data/staging/*.slice.json`
- Выход: `/data/out/ConceptDictionary.json`

**Детали**: `/docs/specs/cli_itext2kg_concepts.md`

#### iText2KG Graph

**Что делает**: Строит граф знаний, используя готовый словарь концептов. LLM создаёт временные ID (chunk_1), которые автоматически заменяются на позиционные.

**Почему важен**: Устраняет проблему неправильного вычисления позиций LLM. Автоматическое создание MENTIONS связей на основе словаря.

**Вход/выход**:
- Вход: слайсы + ConceptDictionary
- Выход: `/data/out/LearningChunkGraph_raw.json`

**Детали**: `/docs/specs/cli_itext2kg_graph.md`

#### Dedup

**Что делает**: Находит и устраняет семантически "идентичные" узлы через векторные представления (embeddings) и FAISS.

**Почему важен**: Убирает дубликаты, возникающие при перекрытии слайсов или повторении материала в разных главах.

**Вход/выход**:
- Вход: LearningChunkGraph_raw
- Выход: `/data/out/LearningChunkGraph_dedup.json`

**Детали**: `/docs/specs/cli_dedup.md`

#### Refiner Longrange

**Что делает**: Обогащает граф связями между узлами из разных контекстов через двухпроходный анализ (forward/backward).

**Почему важен**: Выявляет дальние зависимости между концептами, разделёнными десятками страниц. Критично для понимания межтематических отношений.

**Вход/выход**:
- Вход: LearningChunkGraph_dedup
- Выход: `/data/out/LearningChunkGraph_longrange.json`

**Детали**: `/docs/specs/cli_refiner_longrange.md`

### 1.2. Форматы данных

#### ConceptDictionary

Структурированный глоссарий всех доменных понятий с определениями и алиасами.

**Пример словаря:**
```json
{
  "concepts": [
    {
      "concept_id": "algo:p:sorting",
      "term": {
        "primary": "Сортировка",
        "aliases": ["sorting", "упорядочивание", "sort algorithm"]
      },
      "definition": "Процесс упорядочивания элементов коллекции согласно определенному критерию"
    },
    {
      "concept_id": "algo:p:quicksort",
      "term": {
        "primary": "Быстрая сортировка",
        "aliases": ["quicksort", "quick sort", "сортировка Хоара"]
      },
      "definition": "Алгоритм сортировки, использующий стратегию разделяй и властвуй с выбором опорного элемента"
    }
  ]
}
```

**JSON Schema**: `/src/schemas/ConceptDictionary.schema.json`

#### LearningChunkGraph

Граф знаний с тремя типами узлов и девятью типами образовательных связей.

**Типы узлов**:
- **Chunk**: Учебный фрагмент (абзац, пример, объяснение)
- **Concept**: Доменный концепт из словаря
- **Assessment**: Упражнение, вопрос или задание

**Типы связей (с образовательной семантикой)**:
- **PREREQUISITE**: Необходимо понять A перед B
- **ELABORATES**: B детализирует A
- **EXAMPLE_OF**: A является примером B
- **PARALLEL**: Альтернативные подходы к одной теме
- **TESTS**: Задание проверяет понимание
- **REVISION_OF**: Обновленная или исправленная версия
- **HINT_FORWARD**: Предварительный обзор будущего материала
- **REFER_BACK**: Ссылка на ранний материал
- **MENTIONS**: Фрагмент упоминает концепт

**Пример графа:**
```json
{
  "nodes": [
    {
      "id": "chunk_042",
      "type": "Chunk",
      "text": "Рассмотрим алгоритм быстрой сортировки. Основная идея - выбрать опорный элемент...",
      "definition": "Описание алгоритма quicksort",
      "difficulty": 3,
      "node_offset": 15420
    },
    {
      "id": "algo:p:quicksort",
      "type": "Concept",
      "text": "Быстрая сортировка",
      "definition": "Алгоритм сортировки O(n log n) в среднем случае"
    }
  ],
  "edges": [
    {
      "source": "algo:p:sorting",
      "target": "chunk_042",
      "type": "PREREQUISITE",
      "weight": 0.9,
      "conditions": "Нужно понимать общие принципы сортировки перед изучением конкретного алгоритма"
    },
    {
      "source": "chunk_042",
      "target": "algo:p:quicksort",
      "type": "ELABORATES",
      "weight": 0.95
    }
  ]
}
```

**JSON Schema**: `/src/schemas/LearningChunkGraph.schema.json`

### 1.3. Технические особенности

**Управление контекстом LLM**:
- Использование `previous_response_id` для цепочек контекста
- Автоматическое ограничение глубины цепочки (через `/src/config.toml`)
- Удаление старых ответов через OpenAI API при превышении лимита (снижение стоимости)

**Инкрементальность и детерминизм**:
- Обработка больших текстов (1000+ страниц) по частям
- Детерминированная генерация ID для повторяемости
- Семантическое разбиение с сохранением границ

**Семантическая дедупликация** - удаление избыточного контента через:
- OpenAI embeddings (text-embedding-3-small)
- FAISS для поиска ближайших соседей
- Кластеризация по косинусному сходству
- Union-Find для транзитивных отношений
- Сохранение лучшей версии дубликатов

**Обнаружение дальних зависимостей**:
- Двухпроходный анализ (forward/backward)
- Семантический поиск по embeddings
- Валидация типов связей через LLM
- Веса на основе семантической силы

**Валидация на каждом шаге**:
- JSON Schema validation для всех выходных файлов
- Repair-reprompt механизм для исправления ошибок LLM
- Exit codes для консистентной обработки ошибок

**Промпты в `/src/prompts/`**:
- `itext2kg_concepts_extraction.md` - извлечение концептов
- `itext2kg_graph_extraction.md` - построение графа
- `refiner_longrange_fw.md` - forward проход
- `refiner_longrange_bw.md` - backward проход

**Настройки основного pipeline `/src/config.toml`**:
- Параметры нарезки текста (chunk size, overlap)
- Модели OpenAI и лимиты API
- Управление контекстом LLM (chain depth)
- Параметры дедупликации и refiner
- **Важно**: содержимое конфига всегда актуальнее документации

**Детали**: спецификации всех модулей утилит `/docs/specs/cli_*.md` и `/docs/specs/util_*.md`

### 1.4. Quick Start

```
# 1. Подготовить контент в /data/raw/

# 2. Запустить pipeline
python -m src.slicer              # Нарезка на слайсы
python -m src.itext2kg_concepts   # Извлечение концептов
python -m src.itext2kg_graph      # Построение графа
python -m src.dedup               # Дедупликация
python -m src.refiner_longrange   # Дальние связи

# 3. Результаты в /data/out/
ConceptDictionary.json
LearningChunkGraph_raw.json
LearningChunkGraph_dedup.json
LearningChunkGraph_longrange.json
```

**Подробные инструкции**: в спецификациях модулей утилит `/docs/specs/util_*.md`

## ЧАСТЬ 2. ИНСТРУМЕНТЫ ИССЛЕДОВАТЕЛЯ

### 2.1. Зачем обогащать граф

**Проблема**: JSON файл с тысячами узлов и рёбер невозможно анализировать вручную. Как понять структуру знаний? Какие концепты фундаментальные? Где пробелы в prerequisite chains? Хорошо ли выделены концепции и связи между ними?

**Решение**: Вычисление образовательных метрик, выявляющих скрытую структуру графа, + интерактивные инструменты визуализации и исследования.

**Для кого**:
- **Методисты**: контроль качества, валидация учебных траекторий
- **Исследователи**: анализ структуры знаний, поиск паттернов
- **Стейкхолдеры**: презентация результатов через интерактивный граф

### 2.2. Pipeline визуализации

```
🕸️ Knowledge Graph (with Concept Dictionary)
    ↓
1. Graph2metrics      → Enriched Graph with 12 metrics
    ↓
2. Anomaly Detector   → Quality report + validation
    ↓
3. Graph Fix          → LLM content marking & concepts enrichment
    ↓
4. Graph2html         → Interactive Graph (knowledge_graph.html)
5. Graph2viewer       → Detailed Viewer (knowledge_graph_viewer.html)
    ↓
📊 Research Tools
```

- **Graph2metrics**: Вычисляет 12 образовательных метрик (centrality, importance, navigation) → `/docs/specs/viz_graph2metrics.md`
- **Anomaly Detector**: Проверяет инварианты (sum PageRank=1.0), находит outliers, генерирует отчёт → `/docs/specs/viz_anomaly_detector.md`
- **Graph Fix**: Маркирует LLM-сгенерированный контент тегом `[added_by=LLM]` для QA → `/docs/specs/viz_graph_fix.md`
- **Graph2html**: Создаёт интерактивную визуализацию с Cytoscape.js → `/docs/specs/viz_graph2html.md`
- **Graph2viewer**: Генерирует 3-колоночный табличный интерфейс для детального анализа → `/docs/specs/viz_graph2viewer.md`

### 2.3. Образовательные метрики (12 метрик)

**Структурные метрики**:
1. **degree_in**: Входящая степень - количество зависимостей (от чего зависит узел)
2. **degree_out**: Исходящая степень - что открывает узел (на что влияет)
3. **degree_centrality**: Нормализованная связность относительно размера графа
4. **component_id**: Компонента связности (изолированные тематические области)

**Метрики важности**:
5. **pagerank**: Общая важность с учетом того, кто на узел ссылается
6. **betweenness_centrality**: Узел как мост между разными частями (критические коннекторы)
7. **out-closeness**: Насколько легко из узла достичь другого контента
8. **educational_importance**: PageRank только по образовательным связям

**Навигационные метрики**:
9. **prerequisite_depth**: Уровень в дереве зависимостей (0 = основы, выше = продвинутые)
10. **learning_effort**: Накопленная сложность с учетом всех предпосылок

**Метрики кластеризации**:
11. **cluster_id**: Тематическая группа (алгоритм Louvain)
12. **bridge_score**: Насколько узел связывает разные кластеры

#### Пример узла с метриками

```json
{
  "id": "algo:p:quicksort",
  "degree_in": 3,                  // Зависит от 3 концептов
  "degree_out": 7,                 // Открывает 7 новых тем
  "degree_centrality": 0.042,      // 4.2% от максимальной связности
  "pagerank": 0.0234,              // Высокая важность в графе
  "betweenness_centrality": 0.15,  // Мост между 15% путей
  "out-closeness": 0.73,           // Легко ведёт к другим темам
  "educational_importance": 0.028, // Очень важен для обучения
  "component_id": 0,               // Главная компонента
  "prerequisite_depth": 3,         // Уровень 3: основы → сортировка → разделяй-властвуй
  "learning_effort": 42.5,         // Суммарная сложность с предпосылками
  "cluster_id": 7,                 // Кластер "Продвинутые алгоритмы"
  "bridge_score": 0.85             // Сильно связывает теорию с практикой
}
```

**Детали алгоритмов**: `/docs/specs/viz_metrics_reference.md`

### 2.4. Качество данных

#### Anomaly Detector

Комплексная проверка корректности обогащённого графа перед визуализацией:

**Критические проверки** (остановка при ошибке):
- Инварианты: `sum(PageRank) = 1.0 ± 0.01`
- Последовательность `component_id` от 0
- Отсутствие NaN/Inf значений
- Наличие всех обязательных метрик

**Статистический анализ**:
- Детекция выбросов (IQR или 3-sigma методы)
- Проверка кластеризации и bridge_score
- Статистика по всем метрикам (min/max/mean/std)

**Выход**:
- Консольный отчёт с индикаторами
- JSON отчёт в `/viz/logs/anomaly_report_TIMESTAMP.json`

**Детали**: `/docs/specs/viz_anomaly_detector.md`

#### Graph Fix

Маркирует LLM-сгенерированный контент для QA методистами:

**Что маркируется**:
- Определения Chunk/Assessment узлов → `[added_by=LLM]`
- Условия рёбер (`conditions`) → `[added_by=LLM]`
- Обновление `text` в Concept узлах из словаря (добавление альясов)

**Зачем**: позволяет методистам быстро найти контент, требующий проверки человеком.

**Идемпотентность**: повторный запуск не добавляет дубликатов маркеров.

**Детали**: `/docs/specs/viz_graph_fix.md`

### 2.5. Инструменты визуализации

#### Interactive Graph (`knowledge_graph.html`)

**Для кого**: презентации, первичное исследование, демонстрация стейкхолдерам

**Основные возможности**:
- Визуальный граф с Cytoscape.js (drag, zoom, pan)
- Автоматическая кластеризация с цветовым кодированием
- Словарь концептов с упоминаниями
- TOP узлы по важности, betweenness, degree
- Содержание курса (последовательность Chunks)

**Продвинутые режимы** (в Beta):
- 🛤️ **Path Mode**: поиск учебных путей между узлами
- 🎨 **Clusters Mode**: визуализация тематических групп

**Техническая основа**: единый HTML файл с встроенными данными, работает offline

**Детали**: `/docs/specs/viz_graph2html.md`

#### Detailed Viewer (`knowledge_graph_viewer.html`)

**Для кого**: методисты, детальный анализ качества графа

**Интерфейс** (3 колонки):

**Колонка A - Поиск и выбор**:
- Real-time поиск по всем узлам
- Фильтр по типу (Chunk/Concept/Assessment)
- Сортировка: Concepts алфавитно, Chunks/Assessments по позиции
- Цветовая индикация кластера

**Колонка B - Активный узел**:
- Переключение: форматированный вид ⇄ raw JSON
- Полный контент с markdown/code/math рендерингом
- Все 12 метрик с образовательными пояснениями (при клике на ℹ️)
- Таблица связей (кликабельна для перехода к связанному узлу)

**Колонка C - Связанный узел**:
- Визуализация связи между B и C (стрелка, тип, вес)
- Полная информация о связанном узле
- Кнопка "← Сделать активным" для навигации

**История навигации**: последние 5 узлов с возможностью перехода

**Детали**: `/docs/specs/viz_graph2viewer.md`

### 2.6. Примеры использования

#### Поиск учебных путей (Learning Path)

Вычисляет оптимальные маршруты обучения между любыми двумя темами:

- **Быстрый путь**: минимум шагов через сильные связи (PREREQUISITE, ELABORATES, TESTS)
- **Простой путь**: учитывает сложность узлов (`difficulty`) и веса связей

**Пример маршрута**:
```
Переменные → Функции → Структуры данных → Алгоритмы → 
Статистика → Линейная алгебра → Нейронные сети → Машинное обучение

(8 шагов, суммарная сложность: 127.5)
```

**Особенности**:
- Соблюдает цепочки предпосылок
- Выявляет "слабые звенья", требующие дополнительной поддержки
- Адаптируется к уровню студента

#### Кластеризация знаний

Автоматическая группировка связанного контента через алгоритм Louvain:

- Идентификация тематических блоков
- Поиск узлов-мостов между темами
- Помогает организовать учебный план в модули

**Визуализация**: кластеры окрашиваются разными цветами, межкластерные связи выделяются пунктиром.

#### Поиск пробелов в знаниях

```python
# Студент завершил узлы: [chunk_001, chunk_002, chunk_005]
# Цель: algo:p:quicksort
# Система выявляет недостающие предпосылки:
# - algo:p:sorting (prerequisite_depth: 1)
# - algo:p:divide-conquer (prerequisite_depth: 2)
```

#### Адаптивный маршрут обучения

```python
# Для отстающего студента (предпочтение difficulty ≤3):
path = find_path(
    source="переменные", 
    target="рекурсия", 
    max_difficulty=3
)
# Возвращает более длинный, но простой путь через больше примеров

# Для продвинутого студента:
path = find_path(
    source="переменные", 
    target="рекурсия", 
    minimize_steps=True
)
# Возвращает прямой путь только через ключевые концепты
```

#### Рекомендации контента

```python
# После завершения "Пузырьковая сортировка":
next_topics = [
    ("Сортировка выбором", 0.8, "PARALLEL"),     # Альтернативный подход
    ("Быстрая сортировка", 0.7, "ELABORATES"),   # Более продвинутая
    ("Сложность сортировок", 0.9, "TESTS"),      # Проверка понимания
]
```

### 2.7. Технические особенности

**Основные инструменты**:
- Графовые алгоритмы: Python: NetworkX, python-louvain
- Генерация HTML: Jinja2, minify-html
- JavaScript (embedded in HTML):
  - Cytoscape.js - graph visualization
  - Marked.js - markdown parsing
  - Highlight.js - code syntax highlighting
  - MathJax - mathematical formula rendering

**Настройки визуализации `/viz/config.toml`**:
- Алгоритмы метрик (PageRank damping, Louvain resolution)
- Параметры визуализации (цвета, размеры, layouts)
- Настройки Path Mode и Clusters
- Конфигурация Anomaly Detector
- **Важно**: содержимое конфига всегда актуальнее документации

### 2.8. Quick Start

```
# 1. Копировать финальные файлы из основного pipeline
/data/out/ConceptDictionary.json         → /viz/data/in/ConceptDictionary.json
/data/out/LearningChunkGraph_*.json      → /viz/data/in/LearningChunkGraph.json

# 2. Запустить pipeline визуализации
python -m viz.graph2metrics      # Вычислить метрики
python -m viz.graph_fix          # Маркировать LLM контент
python -m viz.anomaly_detector   # Проверить качество
python -m viz.graph2html         # Создать интерактивный граф
python -m viz.graph2viewer       # Создать детальный просмотровщик

# 3. Открыть результаты в браузере результаты из /viz/data/out/ 
knowledge_graph.html
knowledge_graph_viewer.html
```

**Подробные инструкции**: в спецификациях модулей утилит `/docs/specs/viz_*.md`

## КАРТА ПРОЕКТА

```
k2-18/
├── src/                          # Основной pipeline
│   ├── schemas/                  # JSON схемы (ConceptDictionary, LearningChunkGraph)
│   ├── prompts/                  # Промпты для LLM (extraction, refinement)
│   ├── utils/                    # Вспомогательные модули (llm_client, tokenizer, etc.)
│   ├── slicer.py                 # CLI: нарезка текста
│   ├── itext2kg_concepts.py      # CLI: извлечение концептов
│   ├── itext2kg_graph.py         # CLI: построение графа
│   ├── dedup.py                  # CLI: дедупликация
│   ├── refiner_longrange.py      # CLI: дальние связи
│   └── config.toml               # Конфигурация основного pipeline
│
├── viz/                          # Инструменты визуализации
│   ├── data/
│   │   ├── in/                   # Входные файлы (копии из /data/out/)
│   │   ├── out/                  # Результаты (HTML файлы, обогащённые графы)
│   │   └── test/                 # Эталонные тестовые графы
│   ├── templates/                # Jinja2 шаблоны для HTML
│   ├── static/                   # JavaScript модули (UI logic)
│   ├── vendor/                   # Внешние библиотеки (Cytoscape.js, etc.)
│   ├── graph2metrics.py          # CLI: вычисление метрик
│   ├── anomaly_detector.py       # CLI: валидация качества
│   ├── graph_fix.py              # CLI: маркировка LLM контента
│   ├── graph2html.py             # CLI: интерактивный граф
│   ├── graph2viewer.py           # CLI: детальный просмотровщик
│   ├── config.toml               # Конфигурация визуализации
│   └── README.md                 # Документация модуля
│
├── data/                         # Данные основного pipeline
│   ├── raw/                      # Исходные материалы (.md, .txt, .html)
│   ├── staging/                  # Промежуточные слайсы
│   └── out/                      # Результаты pipeline
│
├── docs/
│   └── specs/                    # Детальные спецификации модулей
│       ├── cli_*.md              # Спеки CLI утилит
│       ├── util_*.md             # Спеки вспомогательных модулей
│       └── viz_*.md              # Спеки модуля визуализации
│
├── tests/                        # Тесты
├── logs/                         # Детальные логи выполнения
├── README.md                     # Основная документация
├── requirements.txt              # Зависимости проекта
└── dev-requirements.txt          # Зависимости проекта для разработки
```